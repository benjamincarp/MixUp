<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>{{ title }}</h1>
    <h3>
    {{#query.name}}
    	Ingredients matching "{{query.name}}"
    {{/query.name}}
    {{^query.name}}
    	All Ingredients:
    {{/query.name}}
    </h3>
    <uL>
    	{{#ingredients}}
    		<li>{{name}}  [{{_id}}]</li>
    	{{/ingredients}}
    </uL>
  </body>
</html>